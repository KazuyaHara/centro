{
  "name": "web",
  "description": "The place where you were.",
  "version": "0.0.1",
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.4",
    "@testing-library/react": "^13.3.0",
    "@testing-library/user-event": "^13.5.0",
    "@types/jest": "^27.5.2",
    "@types/node": "^16.11.45",
    "@types/react": "^18.0.15",
    "@types/react-dom": "^18.0.6",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "typescript": "^4.7.4",
    "web-vitals": "^2.1.4"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "main": "src/index.tsx",
  "private": true,
  "scripts": {
    "build": "react-scripts build",
    "deploy:dev": "yarn build && firebase use default && firebase deploy --only hosting",
    "deploy:prd": "yarn build && firebase use production && firebase deploy --only hosting && firebase use default",
    "deploy:stg": "yarn build && firebase use staging && firebase deploy --only hosting && firebase use default",
    "eject": "react-scripts eject",
    "start": "react-scripts start",
    "test": "react-scripts test"
  }
}
